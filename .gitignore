# Rust
/target
**/target

# Node / frontend
node_modules/
dist/
.vite/
.svelte-kit/
/app/build/

# Models (large, downloaded on demand at runtime — never committed)
/models/
*.bin
*.onnx
*.gguf

# Exception: the Silero VAD model is a small (~600 KB) fixed runtime asset bundled with the app
# (not a large user-swappable ASR model), so it ships in-tree and works offline on first launch.
!/app/src-tauri/resources/silero_vad.onnx

# Env / secrets
.env
.env.*

# OS
.DS_Store
Thumbs.db
